---
item: 7
commit: ec6ffb1
reviewer: R2-quality
spec_ref: tech.md §517
verdict: pass-with-nits
---

# Findings

- The test is idiomatic Rust for an acceptance/contract test in this codebase. `event.payload().expect("event has a payload")` followed by `payload.get("target").and_then(|v| v.as_str()).expect(...)` is a clean Option-chain ending in a panicking assertion, which is standard for unit tests.
- `payload()` API choice is correct: confirmed at `app/src/server/telemetry/events.rs:2895` that `pub fn payload(&self) -> Option<Value>` is an inherent method on `TelemetryEvent`, so no trait import is needed. The trait impl at line 2862 simply delegates. The doc comment in the test correctly notes this routing.
- `cfg` gating matches the surrounding test file. The pre-existing `telemetry_events_have_nonempty_name_and_description` (lines 15-16) is gated `#[cfg(not(target_family = "wasm"))]`. The new test adds `#[cfg(feature = "local_fs")]` on top, which is justified because `FileTarget::ImagePreview` lives behind `local_fs` (consistent with the resolver gating in items 1a/2). Two stacked `#[cfg(...)]` attributes is idiomatic and equivalent to an `all(...)`.
- Test naming: `code_panels_file_opened_serializes_image_preview_target` — no `test_` prefix, matching the surrounding `telemetry_events_have_nonempty_name_and_description` convention in the same file. Good local consistency. §517 does not prescribe a name, so this is choice; it is well-chosen and self-documenting.
- Comment quality: leading doc comment cites §517 explicitly, names the dashboard-filter rationale, and lists the alternative `target` strings the new variant must remain distinguishable from. This is appropriate density for a contract-locking test where the *why* matters more than the *what*.
- Inline comment about "events.rs:2862" carries minor drift risk but is informational, not load-bearing — the test still works regardless of line number, so the rot is purely cosmetic. Acceptable.
- `use crate::util::openable_file_type::FileTarget;` placed inside the test fn rather than at file scope is fine — it is the only consumer of `FileTarget` in this file, and function-scope `use` is idiomatic when the import is single-use. `super::TelemetryEvent` and `super::CodePanelsFileOpenEntrypoint` follow the same pattern as a typical inline-module test, though there is no `mod tests` wrapper here (events_test.rs is the test file alongside events.rs and is included via `#[path]` or similar). Verified consistent with the existing test at the top of the file.
- Test rigor — what is asserted: `payload["target"] == "image_preview"` (string match). This pins the contract §517 cares about. A future change of the serialized form to `"image-preview"`, `"imagePreview"`, etc. fails this test. Good.
- Test rigor — what is *not* asserted: the test does not assert payload shape exhaustively (e.g. that `entrypoint` is also serialized, or that no extra fields leak). The pre-existing pattern in `app/src/ai/agent_sdk/mod_tests.rs:80` uses `assert_eq!(event.payload(), Some(json!({ ... })))` which pins the entire payload — a stricter contract. §517 says "no additional event, no additional enum, no aggregation change" but does *not* explicitly forbid additive payload fields, and the diff comment in this commit even acknowledges that additive fields (extension, size bucket) may come later. So the looser assertion is consistent with §523 ("additive changes ... out of scope here"). Defensible, but a follow-up audit could tighten this if dashboards become sensitive to extra fields.
- Path leak: `FileTarget::ImagePreview` is a unit variant in the §517-described shape; it carries no path data. The `target = "image_preview"` string assertion confirms no path leaks through this field. ✓
- Cross-entrypoint coverage: the test exercises `ProjectExplorer` only. Other `CodePanelsFileOpenEntrypoint` variants (e.g. `GlobalSearch`, `CodeReview`) are not tested with `ImagePreview`. Per §519 the file-tree path is the only surface that produces `ImagePreview` in v1 (see end-to-end flow §541-549), so single-entrypoint coverage is appropriate scope. Not a gap.
- Bullet narrowing in the same commit: `IMPLEMENTATION_TODO.md` was edited in the same commit from "Telemetry events for image-preview open / error / cap-hit" to "Telemetry serialization test for the existing CodePanelsFileOpened event". Same-commit narrowing is the kind of thing R1 flagged on item 3b. Here it is justified by §517 explicitly forbidding new events ("No telemetry-enum change. ... no additional event, no additional enum, no aggregation change"), and the new bullet text quotes that constraint inline. The narrowing is faithful to spec, not a scope reduction. Acceptable.
- One minor inconsistency: the new bullet text says "wired in item 1a", but the commit body credits commit `fa5336b` for the `FileTarget::ImagePreview => ("image_preview", None, None)` arm. Worth the reviewer cross-checking that 1a and `fa5336b` refer to the same change; if they do this is fine, if they do not the bullet text is slightly off.

# What I checked

- `git show ec6ffb1` — read full commit diff and message.
- `app/src/server/telemetry/events_test.rs` lines 1-53 — confirmed the new test is appended after one pre-existing test and follows the file's `#[cfg(not(target_family = "wasm"))]` convention.
- `app/src/server/telemetry/events.rs` lines 2857-2900 — confirmed `pub fn payload(&self) -> Option<Value>` is an inherent method on `TelemetryEvent` and the `TelemetryEventTrait::payload` impl delegates to it, so the inherent-method call site requires no trait import.
- `specs/GH9729/tech.md` lines 517-605 — confirmed §517 explicitly forbids enum/event/aggregation changes; the test contract matches the spec's "stable string `image_preview`" promise; §595-597 ("Telemetry distinguishability") names the unit test as the validation hook.
- `app/src/ai/agent_sdk/mod_tests.rs` lines 70-99 — surveyed an existing similar payload-assertion pattern in the codebase (`assert_eq!(event.payload(), Some(json!({...})))`) to compare assertion strictness.
- `app/src/server/telemetry/macros.rs` and `app/src/server/telemetry/mod.rs` — confirmed `event.payload()` is the standard production call path, so the test exercises the same surface dashboards consume.

# Suggestions

- (Nit, optional) Tighten the assertion to pin the full payload shape via `assert_eq!(event.payload(), Some(json!({ "entrypoint": "project_explorer", "target": "image_preview" })));` matching the `agent_sdk/mod_tests.rs:80` pattern. This catches accidental field leaks (e.g. a future implementer adding `path` to the payload). The current assertion is sufficient for §517's stated criterion, so this is a strengthening, not a fix.
- (Nit, optional) Drop the line-number reference `events.rs:2862` from the inline comment to avoid drift; "an inherent method on `TelemetryEvent`" alone is sufficient.
- (Nit, optional) If "1a" and commit `fa5336b` are not the same change, fix the IMPLEMENTATION_TODO bullet text to credit the correct item.
- No blocking changes. The test correctly locks the §517 contract.
