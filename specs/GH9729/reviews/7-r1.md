---
item: 7
commit: ec6ffb1
reviewer: R1-correctness
spec_ref: tech.md §517
verdict: pass-with-nits
---

# Findings

- **Spec fidelity (pass).** §519 says "No telemetry-enum change. The new `FileTarget::ImagePreview` variant flows through the existing `TelemetryEvent::CodePanelsFileOpened { entrypoint: ProjectExplorer, target }` event ...". §521 prescribes the concrete acceptance criterion: "a click on `screenshot.png` produces a `CodePanelsFileOpened` event with `target = \"image_preview\"` ... No additional event, no additional enum, no aggregation change." The commit implements exactly that acceptance criterion, no more. The bullet narrowing in `IMPLEMENTATION_TODO.md` from "open / error / cap-hit" to "serialization test for the existing event" is fully justified by §519/§521 and §595-597 ("Telemetry distinguishability") which explicitly defer error/cap-hit eventing.
- **Test correctness (pass).** Constructs `CodePanelsFileOpened { entrypoint: ProjectExplorer, target: ImagePreview }`; calls inherent `payload()` (confirmed at `events.rs:2862` and the matching arm at `events.rs:3245-3265`); extracts `payload["target"].as_str()`; asserts `"image_preview"`. Matches the serialization arm `FileTarget::ImagePreview => ("image_preview", None, None)` and the surrounding `json!({"entrypoint": entrypoint, "target": target, "layout": layout, "editor": editor})` shape.
- **Stable-string contract locked (pass).** `"image_preview"` is the dashboard filter key. The test pins it; any future rename of the arm is caught at presubmit. This is the entire point of §521's audit.
- **`#[cfg]` gates are correct.** `#[cfg(feature = "local_fs")]` is required because the entire `CodePanelsFileOpened` payload arm at `events.rs:3245` is `#[cfg(feature = "local_fs")]`-gated, and the `CodePanelsFileOpenEntrypoint` enum at `events.rs:481-488` is itself `#[cfg(feature = "local_fs")]`-gated. Without the gate, the test would not compile in non-local_fs builds. `#[cfg(not(target_family = "wasm"))]` mirrors the sibling test `telemetry_events_have_nonempty_name_and_description` (line 16) and matches local convention; the gate is conservative but consistent with the file's pattern.
- **Coverage scope (acceptable).** The test covers only the `ImagePreview` arm. Other arms (`MarkdownViewer`, `CodeEditor`, `EnvEditor`, `SystemDefault`, `SystemGeneric`, `ExternalEditor`) are NOT covered here, and a search of the codebase did not turn up pre-existing serialization tests for those arms either. That is fine for the §517 scope (which targets only the new `ImagePreview` string), but it does mean this is the **first** stable-string lock for `CodePanelsFileOpened`. A regression in another arm's stable string would still slip through. Out of scope for item 7; flagging for awareness.
- **Entrypoint coverage (acceptable).** Only `ProjectExplorer` is exercised. Since target serialization is independent of entrypoint in the matched arm (the `match target { ... }` does not branch on `entrypoint`), one entrypoint suffices to lock the target string. ✓
- **Privacy posture of the telemetry payload (pass).** The `payload()` arm emits `entrypoint` (enum), `target` ("image_preview"), `layout` (None for ImagePreview), `editor` (None for ImagePreview). No file path, no filename, no size, no decoded bytes. ✓
- **Telemetry-adjacent log leak (nit, out-of-scope of this commit).** `app/src/workspace/lightbox_view.rs:203` emits `log::warn!("GH9729: image preview load failed: {}", err)` where `err: Rc<anyhow::Error>` from `AssetState::FailedToLoad`. The asset-cache load future opens the file by path, so the error chain typically formats with the offending path embedded. This is not a *telemetry* leak (it does not flow into `TelemetryEvent`), but it is operator-visible logging that contains a user file path. Out of scope for item 7's acceptance test, and §517-523 makes no claim about logs, but worth flagging to R2/R3 (security/privacy) as a follow-up: either redact the path before logging, or accept the operator-log path-disclosure as the same posture other `view.rs:2486/2524/2539` warns already adopt.

# What I checked

- `git show ec6ffb1` — diff is +25 lines in `events_test.rs` plus the bullet-narrowing in `IMPLEMENTATION_TODO.md`. No other file touched.
- `specs/GH9729/tech.md` §517-523 (the entire "7. Telemetry" section) for spec fidelity and acceptance-criterion phrasing.
- `app/src/server/telemetry/events.rs:2862` — confirmed `pub fn payload(&self) -> Option<Value>` is an inherent method (matches the comment in the test).
- `app/src/server/telemetry/events.rs:3245-3265` — confirmed the `CodePanelsFileOpened` arm matches on `FileTarget::ImagePreview => ("image_preview", None, None)` and emits `{entrypoint, target, layout, editor}`.
- `app/src/server/telemetry/events.rs:481-488` — confirmed `CodePanelsFileOpenEntrypoint` is `#[cfg(feature = "local_fs")]`-gated, justifying the test's local_fs gate.
- `app/src/server/telemetry/events_test.rs` — confirmed the `wasm` gate matches the sibling test's convention and the test compiles standalone (only one `use` import for `FileTarget`; `super::TelemetryEvent` and `super::CodePanelsFileOpenEntrypoint` are properly qualified).
- `app/src/util/openable_file_type.rs:42-62` — confirmed `FileTarget::ImagePreview` is a unit variant with no payload (no `EditorLayout`), which is why the serialization arm emits `(_, None, None)`.
- Searched for log statements in GH9729-touched files (`file_tree/view.rs`, `lightbox_view.rs`, `openable_file_type.rs`) for telemetry-adjacent path leaks; found one at `lightbox_view.rs:203` (flagged above).
- Searched for pre-existing `CodePanelsFileOpened` serialization tests; only this commit's test exists.

# Suggestions

- (nit, optional) Consider extending the test in a follow-up to also assert `payload["entrypoint"] == "project_explorer"` so the `serde(rename_all = "snake_case")` derive on `CodePanelsFileOpenEntrypoint` is also locked. Out of scope for §517 strictly, but it is one extra line and seals the second dashboard filter key.
- (nit, optional) Consider asserting `payload["layout"].is_null()` and `payload["editor"].is_null()` for the `ImagePreview` arm so a future change that accidentally adds a layout/editor to the `ImagePreview` row (which would be a privacy regression — leaking which pane was active) is caught. Strictly out of scope but a one-line ergonomic improvement.
- (follow-up, separate commit) Audit `lightbox_view.rs:203` `log::warn!(... err)` for path disclosure in operator logs and decide whether to keep or redact. Not a §517 deliverable; flag for R2/R3.
- The bullet-narrowing comment in `IMPLEMENTATION_TODO.md` is appropriately defensive ("§517 explicitly says 'no additional event, no additional enum, no aggregation change'"); no change needed there.
