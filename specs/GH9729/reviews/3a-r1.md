---
item: 3a
commit: c0b5a7c
reviewer: R1-correctness
spec_ref: tech.md §182
verdict: pass
---

# Findings

- **Spec fidelity is exact.** The variant shape `Error { message: String }` matches §182 (lines 187-192) verbatim, including the named field. The enum keeps `#[derive(Clone, Debug)]` unchanged (lightbox.rs:27-32 at this commit). Nothing else is added.
- **Doc-comment matches §179 / §203 rationale.** The doc-comment on the new variant explicitly tells callers: "Underlying OS errors and absolute filesystem paths must NOT be interpolated here; log the original via `log::warn!` for the operator instead." That language tracks §179 ("the underlying OS error is logged via `log::warn!` for the operator but never reaches the UI") and §203 ("never interpolates the raw error or any path"). Good for future reviewers reading the type in isolation.
- **The "two exhaustive matches" claim verified.** Searching every `match` and `if let` over `LightboxImageSource` at `c0b5a7c` yields:
  - `app/src/workspace/lightbox_view.rs:155` (`current_image_native_size`'s match) — explicit arms; the new commit adds the `Error => None` arm. Correct.
  - `crates/ui_components/examples/library.rs:730` (`native_size_for_image`'s match) — explicit arms; the new commit adds the `Error => None` arm. Correct.
  - `crates/ui_components/src/lightbox.rs:159` — `match (current_source, params.current_image_native_size)` with one explicit `Resolved+Some` arm and a `_ =>` catch-all. Compiles. Falls through to `loading_element(appearance)` for `Error` until 3b lands.
  - `app/src/workspace/lightbox_view.rs:100` and `:110` — these are `if let LightboxImageSource::Resolved { .. }`, used solely to kick off an `AssetCache` fetch. For `Error` and `Loading` the `if let` is simply skipped. That is the correct behavior for `Error` (no asset to load), so no change needed.
  - All other references are *constructors* (`Loading`, `Resolved { .. }` in artifacts, blocklist, examples, view_test) and `matches!` assertions on `Loading`/`Resolved`; none are exhaustive matches.
  - Conclusion: the diff's claim is accurate, the compile gate at the time of the commit is sound.
- **User-visible behavior in the 3a-only window is inert because no constructor exists.** I grepped for `LightboxImageSource::Error` constructors at `c0b5a7c` and there are zero. The variant is reachable only as a future state. So the "permanently-loading spinner" concern is theoretical at this commit — the spinner-forever fallthrough in `Lightbox::render`'s `_ => loading_element(appearance)` never fires because nothing produces an `Error`. Even if a downstream branch did, the scrim/close-button/Escape are wired *outside* the central content match (lightbox.rs near line 207-215), so dismissal would still work; only the description (gated on `native_size = Some`) would be hidden. Acceptable transient behavior for the 3a→3b window. Worth noting but not blocking.
- **No security regression introduced by 3a alone.** The artifacts workaround at `app/src/ai/artifacts/mod.rs:362-365` is untouched (still `LightboxImageSource::Loading` + `description: Some("Failed to load")`); 3b's planned retirement of it is correctly deferred. No new construction site for `Error` is added in this commit, so the convention-only nature of "don't interpolate paths" cannot be violated by this change. The risk surface is entirely future-tense, to be re-evaluated at 3b.
- **Type-system soundness for derives.** `String: Clone + Debug`, so `#[derive(Clone, Debug)]` continues to apply. The enum has no `PartialEq`, `Eq`, `Hash`, or serde derives — I confirmed by grepping the file. No callers need them: the only existing inspection patterns are exhaustive `match` and `matches!` on `Loading`/`Resolved`, neither of which require `PartialEq`. Sound.
- **TODO checklist update tracks reality.** `IMPLEMENTATION_TODO.md` flips 3a from `[ ]` to `[x]` and 3b stays `[ ]`. Consistent.

# What I checked

- `git show c0b5a7c` (full diff and commit message).
- `specs/GH9729/tech.md` §182 (lines 182-220) and surrounding §179 for the sanitization rationale.
- `git grep -n "LightboxImageSource" c0b5a7c -- 'crates/**/*.rs' 'app/**/*.rs'` to enumerate every reference at the commit.
- The two truly-exhaustive matches (lightbox_view.rs:155, library.rs:730) at the new arms.
- The `Lightbox::render` central-content match in lightbox.rs (line 159 onward) to confirm `_ =>` catch-all swallows `Error` until 3b.
- The two `if let Resolved` sites in lightbox_view.rs:100 / :110 to confirm `Error` is correctly a no-op there.
- `app/src/ai/artifacts/mod.rs:362-365` to confirm the "Failed to load" workaround is left in place (deferred to 3b as the commit body promises).
- `git grep -n "LightboxImageSource::Error" c0b5a7c` to confirm no construction site exists yet — meaning the spinner-forever fallthrough is unreachable in the 3a-only window.
- Derive list on `LightboxImageSource` (`Clone, Debug` only — no `PartialEq`/`Eq`/serde to break).

# Suggestions

- **Nit (optional):** the doc-comment phrasing "renders a non-blocking error panel inline (see `Lightbox::render`)" is slightly forward-referential — at `c0b5a7c` the render branch does NOT yet exist; it's added in 3b. Two options: (a) leave it as-is (it's accurate one commit later, doc-only churn to fix), (b) add a one-line `// Render arm added in commit 3b.` near the variant. Personally I'd leave it; the doc-comment is what it should be on the final tree, and bisect users will figure it out from the commit body. Non-blocking either way.
- **Defense-in-depth thought for 3b (out of scope here, raising for the tracker):** since the doc-comment-only "do not interpolate paths" is convention-only, a tiny private helper `LightboxImageSource::error(category: ErrorCategory)` taking a closed enum instead of a free-form `String` would make the sanitization a type-system invariant rather than a code-review one. The §203 categorical-strings list (`"could not read image"`, `"could not decode image"`, `"image is too large to preview"`, `"could not detect image format"`) is closed and small — a fit for `enum`. Keeping the public `message: String` field for layout flexibility is fine; just don't expose the constructor in a way that lets new callers pass `format!("failed: {path}: {err}")`. Worth raising on 3b, not 3a.
- **For the tracker:** 3a is a clean, inert, spec-faithful API addition. Pass.
